import React from "react";
import ThemeToggler from "../ThemeToggler";

const Navbar = () => {
    return (
        <div className="py-2">
            <ThemeToggler />
        </div>
    );
};

export default Navbar;
